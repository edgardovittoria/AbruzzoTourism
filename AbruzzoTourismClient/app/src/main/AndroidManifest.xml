<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="univaq.aq.it.abruzzotourism">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AggiungiattivitaActivity"></activity>
        <activity android:name=".ProfiloAttivitaActivity" />
        <activity android:name=".SigninAttivitaActivity" />
        <activity android:name=".ProfiloActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".SigninActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MetodoDiPagamentoActivity" />
        <activity android:name=".RiepilogoPrenotazioneActivity" />
        <activity android:name=".PrenotaActivity" />
        <activity android:name=".DettagliAttivitaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>